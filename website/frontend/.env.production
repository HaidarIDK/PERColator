# Production environment variables for Percolator DEX
# These are used when building the Next.js frontend

# API URL - VPS deployment via domain
NEXT_PUBLIC_API_URL=https://api.percolator-app.com

# WebSocket URL - VPS deployment
NEXT_PUBLIC_WS_URL=wss://api.percolator-app.com/ws

# Server WebSocket URL - same as above
NEXT_PUBLIC_SERVER_WS_URL=wss://api.percolator-app.com/ws

# Solana network
NEXT_PUBLIC_SOLANA_NETWORK=devnet
