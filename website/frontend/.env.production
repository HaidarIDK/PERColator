# Production environment variables for Percolator DEX
# These are used when building the Next.js frontend

# API URL - Vercel deployment (will auto-switch to api.percolator.site once DNS propagates)
NEXT_PUBLIC_API_URL=https://percolator-api.vercel.app

# WebSocket URL - Vercel deployment
NEXT_PUBLIC_WS_URL=wss://percolator-api.vercel.app/ws

# Server WebSocket URL - same as above
NEXT_PUBLIC_SERVER_WS_URL=wss://percolator-api.vercel.app/ws

# Solana network (can be mainnet-beta, devnet, or testnet)
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
